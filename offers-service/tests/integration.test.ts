import app from "../src";
import request from "supertest";
import { getOffers } from "../src/controllers/offerController";

describe("User Functions", () => {
  describe("Always Passing Test", () => {
    it("should always pass", () => {
      expect(true).toBe(true);
    });
  });
});
